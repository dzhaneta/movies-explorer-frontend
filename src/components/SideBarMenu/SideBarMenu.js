import React from "react";

function SideBarMenu({ isOpen }) {
    // functionality

    return (
        <div className="sidebar">
            <button
                className="sidebar__close-button link">
            </button>
        </div>
    );

}

export default SideBarMenu;